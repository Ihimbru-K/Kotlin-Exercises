fun main(args: Array<String>) {
   /*reading strings and characters and performing some
    operations on them */

    val a = readln().toInt() //reads an integer
    val b = readln().first() //reads a character
    println(b.toInt() == a) //testing if number representation of character is identical to the first variable a



}